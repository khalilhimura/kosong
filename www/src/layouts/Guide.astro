---
import Base from "./Base.astro";

const { frontmatter } = Astro.props;
---

<Base title={`${frontmatter.title} — kosong`} description={frontmatter.description}>
  <article class="prose">
    <a class="back" href="/">kosong</a>
    <slot />
  </article>
</Base>
