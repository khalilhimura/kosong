{
  "name": "kosong-www",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The kosong project website.",
  "scripts": {
    "collect": "node scripts/collect.mjs",
    "dev": "npm run collect && astro dev",
    "build": "npm run collect && astro build && node scripts/exempt-emails.mjs && node scripts/check-email-exemptions.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "7.1.3"
  }
}
