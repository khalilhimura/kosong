/target
**/node_modules
**/dist
**/.astro
.DS_Store
*.log

# Local workspace artifacts produced by the CLI during development.
/.kosong/
/kosong.md
**/.wrangler
.dev.vars
.env

# Regenerated by `wrangler types`, which `npm run typecheck` runs first.
apps/api/worker-configuration.d.ts

# Internal product and specification documents. Kept locally, not published.
docs/

# Provider credentials kept locally while setting the service up. A key in a
# file is one `git add -A` away from being a key in a public repository.
resend.md
